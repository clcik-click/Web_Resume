# AGENTS.md

## Scope
Guidance for static content and structured config in `src/data`.

## Data Hygiene
- Keep this folder serializable and UI-agnostic.
- Use clear, stable keys for data consumed by pages/components.
- Fix obvious typos/spelling when touching entries, but avoid semantic rewrites unless requested.
