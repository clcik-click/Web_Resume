# AGENTS.md

## Scope
Guidance for static assets in `public`.

## Asset Rules
- Keep original filenames unless a rename is explicitly requested.
- Use folder grouping already present (`HOME`, `RESUME`, `TAB`, `pdfs`, `videos`, `musics`, `icons`).
- Do not modify binary assets unless the task specifically asks for it.
